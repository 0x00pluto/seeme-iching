# 火山引擎 · 火山方舟 Coding Plan（OpenAI 兼容）
# 快速开始：https://www.volcengine.com/docs/82379/1928261
# 获取 API Key：https://www.volcengine.com/docs/82379/1541594
#
# 重要：Coding Plan 请使用下方 OpenAI 兼容 Base URL（.../api/coding/v3）。
# 不要将 Coding 套餐与通用推理端 .../api/v3 混用，否则可能产生额外费用（以控制台说明为准）。

# ARK_API_KEY: （旧）服务端调用用的 API Key
# 本次改造为“前端直连流式”，请改用下方 VITE_ARK_API_KEY（会进入浏览器包内，存在泄露风险）。
ARK_API_KEY=

# ARK_BASE_URL: （旧）服务端 baseURL
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v3

# ARK_MODEL: （旧）服务端 model
ARK_MODEL=ark-code-latest

# =========================
# 同源流式代理（推荐）
# =========================
# 前端只请求同源：
# - POST /api/interpret/stream
# - POST /api/chat/stream
#
# 因此无需在浏览器侧配置任何 Key；只需要服务端环境变量 ARK_API_KEY。

# SSE_PERIODIC_PING_MS: 可选。同源 SSE 空 delta 心跳间隔（毫秒），默认 12000，范围 3000～120000（建议小于常见 ~60s 空闲超时）。
# SSE_PERIODIC_PING_MS=

# APP_URL: 可选。Vite 注入 process.env.APP_URL（如分享链接）
APP_URL=

# =========================
# Supabase（仅服务端；前端不直连）
# =========================
# Dashboard → Settings → API：Project URL、anon public key、service_role key。
# Vercel：在 Project → Settings → Environment Variables 填入与本地同名变量
#（Production / Preview 按需）；切勿把 service_role 暴露给 Vite 前缀变量。

# 服务端（Express / Vercel api/*）：createServerSupabase()、业务读写库
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# 邮箱魔法链接登录：服务端 signInWithOtp（emailRedirectTo）+ 回调换 Cookie；使用 anon public key（勿用 service_role）
SUPABASE_ANON_KEY=

# 浏览器会话 Cookie（HttpOnly）：随机长字符串，用于 HMAC 签名 `seeme_user_session`
USER_SESSION_SECRET=

# 单次登录最长保留天数（签名 Cookie 的 Max-Age / exp）；默认 7
# AUTH_SESSION_DURATION_DAYS=7

# 可选；魔法链接 emailRedirectTo 的 Origin。不设则从请求头推断（本地一般为 http://localhost:3000）。
# 反代 / 预览域名与浏览器不一致时务必设置，并在 Supabase Dashboard → Authentication → URL Configuration
# 的 Redirect URLs 中加入：`{ORIGIN}/auth/callback`（及生产域名）。
# APP_PUBLIC_ORIGIN=https://your-domain.com
